- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/bin: bin/

- shell:
  - [git submodule update --init --recursive, Installing submodules]
